function odyl_update($node) {
        if($node->type == "one_day"){
	//	dpm($node);
		if($node->status == 0){
			drupal_set_message("Your day has been submitted. You will be noticed when it is published.");
		}
        }
}